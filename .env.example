# Modelo — copie este arquivo para ".env" e cole sua chave real.
# O .env está no .gitignore, então NÃO vai para o GitHub.

# API Key (v3 auth) do TMDB — string de 32 caracteres (hex)
TMDB_API_KEY=cole_sua_chave_aqui
